package com.learn.self.ds.algo.arrays;
/*
    @author -- Kishan C
    #### Union and intersection of Two Sorted(in asc) arrays

    Union Algorithm

    arr1[] = {1.2.3.4.5.6}
    arr2[] = {2,3,7,8}

    1) step 1 take two pointers
       i and j
    2) itterate i and j
        when i & j = 0
        if arr1[i] < arr2[j]
            add arr[i] to newly created array
            increment i;
        else arr1[i] > arr2[j]
            add arr[j] to newly created array
            increment j;
        if arr[i] == arr[j]
            add any and increment smaller array length
*/

public class ArraysExcercise2 {
    public static void main(String[] args) {

    }
}
